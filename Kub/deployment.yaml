apiVersion: apps/v1
kind: Deployment
metadata:
  name: romantic-game
  namespace: babayagaaws-dev
  labels:
    app: romantic-game
spec:
  replicas: 3
  selector:
    matchLabels:
      app: romantic-game
  template:
    metadata:
      labels:
        app: romantic-game
    spec:
      containers:
      - name: romantic-game
        image: babayagaaws/romantic-game:latest  # Use the Docker Hub image
        ports:
        - containerPort: 80